apiVersion: v1
kind: Service
metadata:
  name: task-mcp-server
  namespace: project-task-mcp
  labels:
    app: task-mcp-server
    app.kubernetes.io/part-of: task-management-agent
spec:
  type: ClusterIP
  selector:
    app: task-mcp-server
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
