# Fill in your real API key values before applying.
# Never commit this file with real values — keep it in .gitignore or use a secrets manager.
#
# Apply with: kubectl apply -f deployments/task-agent/secret.yaml
apiVersion: v1
kind: Secret
metadata:
  name: task-agent-secrets
  namespace: project-task-mcp
type: Opaque
stringData:
  GEMINI_API_KEY: "REPLACE_ME"
  OPENAI_API_KEY: "REPLACE_ME"
